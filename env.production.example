# Production Environment Configuration
# Copy this file to .env.production for production deployment

# Backend Configuration
BACKEND_PORT=3001
BACKEND_HOST=your-domain-or-ip.com
BACKEND_URL=https://${BACKEND_HOST}

# Frontend Configuration  
FRONTEND_PORT=3000
FRONTEND_HOST=your-domain-or-ip.com
FRONTEND_URL=https://${FRONTEND_HOST}:${FRONTEND_PORT}

# MongoDB Configuration
# Remove public port exposure - MongoDB is now only accessible within Docker network
# MONGO_PORT=27017
MONGO_USERNAME=admin
MONGO_PASSWORD=your-secure-password-here
MONGO_DATABASE=e-commerce-db
# Connection string is now built in docker-compose.prod.yml with authentication

# Example for IP address:
# BACKEND_HOST=your-domain-or-ip
# FRONTEND_HOST=your-domain-or-ip
# BACKEND_URL=http://${BACKEND_HOST}:${BACKEND_PORT}
# FRONTEND_URL=http://${BACKEND_HOST}:${FRONTEND_PORT}
